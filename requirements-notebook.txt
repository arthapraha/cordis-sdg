# THE NOTEBOOK'S DEPENDENCIES, AND NOTHING ELSE'S.
#
# Registration v9 section 6 item 3 asks for environment pins. Everything that
# makes a mapping decision — the pipeline, the vocabulary build, the crosswalk
# verifier, the guard, the metrics — is PURE PYTHON STANDARD LIBRARY and this
# file is not needed to run any of it, or to reproduce any hash on the chain.
# That is deliberate: a result a reader cannot reproduce without resolving a
# dependency tree is a result behind a gate.
#
# These are here because section 6 items 1 and 4 want an executed notebook with
# figures, and drawing needs a drawing library. Owner's word at cordis-sdg
# seq 244: pinned, notebook only, the pipeline stays standard library, the
# notebook is executed for real, and no cell output is ever written by hand.
#
#     python -m pip install -r requirements-notebook.txt
#     jupyter nbconvert --execute --to notebook --inplace notebooks/cordis-sdg.ipynb
#
# Pinned to what the committed notebook was actually executed with, read from
# pip freeze rather than typed from memory. Python 3.14.7.

ipykernel==7.3.0
jupyter==1.1.1
jupyterlab==4.6.3
matplotlib==3.11.2
nbclient==0.11.0
nbconvert==7.17.1
nbformat==5.11.1
notebook==7.6.2
